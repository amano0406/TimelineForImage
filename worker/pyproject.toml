[project]
name = "timeline-for-image-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "Pillow>=10,<12",
  "pytesseract>=0.3.10,<0.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
